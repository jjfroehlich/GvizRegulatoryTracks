% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contribution-track.R
\name{ScoreSequenceTrack}
\alias{ScoreSequenceTrack}
\alias{DynSeqTrack}
\title{Score-scaled sequence track for Gviz}
\usage{
ScoreSequenceTrack(
  sequence = NULL,
  scores,
  reference = NULL,
  genome = NULL,
  chromosome = NULL,
  start = NULL,
  end = NULL,
  region = NULL,
  importFunction = NULL,
  scoreColumn = "score",
  name = "Score sequence",
  normalization = c("none", "maxabs"),
  ylim = NULL,
  missing = c("error", "zero"),
  colors = .nucleotide_colors(),
  maxBases = 250L,
  display = c("auto", "letters", "signal"),
  font = "roboto_medium",
  showAxis = TRUE,
  baselineColor = "#555555",
  showBoundary = TRUE,
  boundaryColor = "#222222",
  boundaryWidth = 0.7,
  boundaryInset = 0.06,
  negativeAlpha = 0.82,
  signalType = "histogram",
  signalAggregation = "mean",
  signalWindow = "auto",
  signalWindowSize = NULL,
  signalColor = "#59636B",
  signalFill = "#7B858D",
  ...
)

DynSeqTrack(...)
}
\arguments{
\item{sequence}{One \code{Biostrings::DNAString} or character sequence. May be
\code{NULL} for a signal-only track.}

\item{scores}{Numeric basewise scores, a single-chromosome \code{GRanges}, or a
BigWig/bedGraph file path. A range wider than one base represents the same
score at every covered base.}

\item{reference}{Optional Gviz-compatible reference source used when
\code{sequence} is \code{NULL}: a \code{Gviz::SequenceTrack}, \code{BSgenome}, \code{DNAStringSet},
indexed FASTA, or 2bit file.}

\item{genome}{Optional genome identifier passed to \code{Gviz::SequenceTrack}
and the overview \code{Gviz::DataTrack}.}

\item{chromosome}{Chromosome for numeric scores; inferred from \code{GRanges}.}

\item{start}{One-based genomic start for numeric scores; inferred from
\code{GRanges}.}

\item{end}{One-based genomic end for score files. It can be inferred from a
supplied sequence.}

\item{region}{Optional one-range \code{GRanges} selecting a score-file interval.}

\item{importFunction}{Optional function with arguments \verb{(file, selection)}
returning scored \code{GRanges}; useful for external large-BigWig readers.}

\item{scoreColumn}{Score metadata column for \code{GRanges} input.}

\item{name}{Gviz track title.}

\item{normalization}{Either \code{"none"} or within-track \code{"maxabs"} scaling.}

\item{ylim}{Numeric y limits. By default symmetric limits are derived from
the displayed scores.}

\item{missing}{How uncovered positions inside a \code{GRanges} span are handled.}

\item{colors}{Named nucleotide color vector.}

\item{maxBases}{Largest plotting viewport that will render letters.}

\item{display}{Rendering mode: automatic letters/signal switching, forced
letters, or forced signal.}

\item{font}{A font name returned by \code{ggseqlogo::list_fonts(FALSE)}.}

\item{showAxis}{Let Gviz draw its standard numeric y axis in the title panel.
Standard Gviz display parameters such as \code{col.axis}, \code{cex.axis}, and
\code{yTicksAt} can be supplied through \code{...} or changed with
\code{Gviz::displayPars()}.}

\item{baselineColor}{Color of the zero baseline.}

\item{showBoundary}{Draw a vertical boundary at the left edge of the data
panel, aligned with the plotting area of other Gviz tracks.}

\item{boundaryColor, boundaryWidth}{Color and line width of that boundary.}

\item{boundaryInset}{Fraction of track height inset at both ends of the
boundary, leaving a visible gap between adjacent track segments.}

\item{negativeAlpha}{Alpha multiplier for negative glyphs.}

\item{signalType}{Gviz DataTrack type used for the overview.}

\item{signalAggregation}{Gviz aggregation function for overview windows.}

\item{signalWindow}{Gviz window setting, normally \code{"auto"}.}

\item{signalWindowSize}{Optional explicit Gviz window size.}

\item{signalColor, signalFill}{Overview signal border and fill colors.}

\item{...}{Additional Gviz display parameters.}
}
\value{
An object inheriting from both \code{Gviz::CustomTrack} and
\code{Gviz::NumericTrack}.
}
\description{
Construct a track whose nucleotide glyph heights encode signed,
base-resolution scores. The result inherits from both \code{Gviz::CustomTrack}
and \code{Gviz::NumericTrack}: the sequence glyphs use Gviz's custom Grid callback
while Gviz itself draws the quantitative y axis. At wider views the track
delegates rendering to a normal \code{Gviz::DataTrack}, including Gviz's window
aggregation. Supply \code{from}, \code{to}, and \code{chromosome} to \code{Gviz::plotTracks()};
Gviz does not infer plotting limits from tracks with a custom callback.
}
