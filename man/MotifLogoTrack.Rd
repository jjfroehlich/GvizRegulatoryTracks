% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motif-logo-track.R
\name{MotifLogoTrack}
\alias{MotifLogoTrack}
\title{Genomically positioned motif-logo track for Gviz}
\usage{
MotifLogoTrack(
  hits,
  motifs,
  motifIdColumn = NULL,
  labelColumn = NULL,
  matrixType = c("PPM", "PCM"),
  name = "Motifs",
  colors = .nucleotide_colors(),
  laneGap = 0.3,
  motifHeight = 10,
  motifColorBy = c("nucleotide", "motif_id"),
  motifPalette = NULL,
  showLabels = FALSE,
  showStrand = FALSE,
  reverseComplementMinus = TRUE,
  showAxis = TRUE,
  showBoundary = TRUE,
  boundaryColor = "#222222",
  boundaryWidth = 0.7,
  boundaryInset = 0.06,
  maxBases = 1000L,
  display = c("auto", "logo", "ranges"),
  font = "roboto_medium",
  rangeFill = "#7A5E8E",
  rangeBorder = "#4A3A57",
  rangeAlpha = 0.75,
  maxRangeLabels = 50L,
  scoreColumn = NULL,
  scoreAesthetic = "none",
  scoreLimits = "view",
  scorePalette = NULL,
  scoreColor = "#111111",
  scoreBrightness = c(0.85, 0),
  scoreOpacity = c(0.25, 1),
  scoreBorderWidth = 0.7,
  showScoreLegend = TRUE,
  scoreLegendTitle = NULL,
  scoreLegendWidth = 16,
  scoreLegendHeight = 23,
  scoreLegendDigits = 3L,
  scoreLegendColor = "#444444",
  ...
)
}
\arguments{
\item{hits}{Single-chromosome \code{GRanges} of motif matches or a BED file path.}

\item{motifs}{Named list of PPM/PCM matrices, \code{universalmotif} object(s), or
a MEME file path.}

\item{motifIdColumn}{Metadata column in \code{hits} matching \code{names(motifs)}.
\code{NULL} automatically checks \code{motif_id}, BED \code{name}, and \code{pattern_name}.}

\item{labelColumn}{Optional metadata column used for displayed hit labels.
\code{NULL} retains motif IDs, preserving the existing default.}

\item{matrixType}{Interpretation of plain matrices, \code{"PPM"} or \code{"PCM"}.}

\item{name}{Gviz track title.}

\item{colors}{Named nucleotide color vector.}

\item{laneGap}{Gap between overlapping-hit lanes, expressed relative to the
two-bit logo height. The default \code{0.30} becomes 15 percent of
\code{motifHeight}.}

\item{motifHeight}{Maximum physical height in millimetres of a full two-bit
motif stack. Logos retain this height when the Gviz track is taller and
shrink uniformly only when the allocated track is too short for all lanes.
Use \code{sizes} in \code{Gviz::plotTracks()} to allocate more room to tracks with
several overlapping lanes.}

\item{motifColorBy}{Fill detailed logos and overview ranges by nucleotide or
by resolved motif ID. Motif IDs may originate from \code{motif_id}, BED \code{name},
\code{pattern_name}, or \code{motifIdColumn}.}

\item{motifPalette}{Named motif-ID colors, or an unnamed vector with at least
one color per motif. \code{NULL} uses a deterministic categorical HCL palette.}

\item{showLabels}{Label each logo with its motif ID.}

\item{showStrand}{Append \verb{(+)} or \verb{(-)} to motif labels, making orientation
explicit in dense multi-motif examples.}

\item{reverseComplementMinus}{Reverse-complement PPMs for minus-strand hits.
The default displays each match in reference-genome orientation and also
accounts for Gviz's \code{reverseStrand = TRUE} axis reversal.}

\item{showAxis}{Draw a 0--2 bit y axis.}

\item{showBoundary}{Draw a vertical boundary at the left edge of the data
panel.}

\item{boundaryColor, boundaryWidth}{Color and line width of that boundary.}

\item{boundaryInset}{Fraction of track height inset at both ends of the
boundary, leaving a visible gap between adjacent track segments.}

\item{maxBases}{Largest plotting viewport that will render logos.}

\item{display}{Rendering mode: automatic logo/range switching, forced logo,
or forced ranges.}

\item{font}{A font name returned by \code{ggseqlogo::list_fonts(FALSE)}.}

\item{rangeFill, rangeBorder, rangeAlpha}{Appearance of overview ranges.}

\item{maxRangeLabels}{Maximum number of overview ranges that may be labeled.}

\item{scoreColumn}{Numeric hit metadata column used for confidence styling.
Numeric factor and character columns are parsed strictly.}

\item{scoreAesthetic}{\code{"none"} or one or more of \code{"fill"}, \code{"border"},
\code{"opacity"}, and \code{"brightness"}. Styling is applied consistently to logos
and overview ranges. Score fill and motif-ID fill are mutually exclusive;
combine motif-ID fill with brightness, opacity, or border instead.}

\item{scoreLimits}{Numeric fixed score limits, \code{"view"} to use the minimum
and maximum among currently visible hits, or \code{"global"} to use all hits in
the track. View scaling is the default; use fixed limits when comparing
score encodings across panels.}

\item{scorePalette}{Optional explicit sequential low-to-high colors for fill
or border mapping. \code{NULL} derives shades from \code{scoreColor}.}

\item{scoreColor}{Base color used for score-mapped fill or border when
\code{scorePalette = NULL}.}

\item{scoreBrightness}{Fractions of white mixed into a base color at the low
and high score limits. This controls \code{"brightness"} and the default
single-color fill/border gradient.}

\item{scoreOpacity}{Low-to-high alpha range for opacity mapping.}

\item{scoreBorderWidth}{Border width for logo glyphs.}

\item{showScoreLegend}{Draw a compact continuous score legend at the right
of the data panel whenever score styling is active.}

\item{scoreLegendTitle}{Legend title. \code{NULL} uses \code{scoreColumn}.}

\item{scoreLegendWidth, scoreLegendHeight}{Minimum legend width and fixed
legend height in mm. Width expands automatically to contain the title;
height is capped when a track is shorter and does not grow with the track.}

\item{scoreLegendDigits}{Significant digits used for legend tick labels.}

\item{scoreLegendColor}{Neutral legend fill when score controls brightness,
opacity, or border while motif identity controls the logo fill.}

\item{...}{Additional Gviz display parameters.}
}
\value{
A \code{Gviz::CustomTrack}.
}
\description{
Place information-content sequence logos over exact motif-hit ranges. Logos
are reverse-complemented for minus-strand hits, and overlapping hits are
placed into deterministic lanes.
}
