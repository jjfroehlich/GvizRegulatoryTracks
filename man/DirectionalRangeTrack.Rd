% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/directional-range-track.R
\name{DirectionalRangeTrack}
\alias{DirectionalRangeTrack}
\title{Fixed-tip directional genomic ranges for Gviz}
\usage{
DirectionalRangeTrack(
  hits,
  name = "Directional ranges",
  featureColumn = NULL,
  labelColumn = NULL,
  fill = "#4C78A8",
  border = "#333333",
  triangleWidth = 1.5,
  rangeHeight = 0.62,
  geometry = c("pointed", "rectangle"),
  directionIndicator = c("none", "arrow"),
  directionColor = "#F3F3F1",
  directionFontsize = 5,
  stacking = c("squish", "dense"),
  showLabels = FALSE,
  labelPosition = c("inside", "above"),
  labelFontsize = 6,
  labelColor = "#111111",
  alpha = 1,
  borderWidth = 0.6,
  showBoundary = TRUE,
  boundaryColor = "#222222",
  boundaryWidth = 0.7,
  boundaryInset = 0.06,
  ...
)
}
\arguments{
\item{hits}{A non-empty, single-chromosome \code{GRanges}.}

\item{name}{Gviz track title.}

\item{featureColumn}{Optional metadata column used to map \code{fill} colors.
When \code{NULL}, \code{feature} is used if present; otherwise all ranges use one
feature.}

\item{labelColumn}{Optional metadata column used for labels. When \code{NULL},
the feature values are used.}

\item{fill}{One color, a named feature-color vector, or an unnamed vector
containing at least one color per feature.}

\item{border, boundaryColor, labelColor}{Colors for range outlines, the left
track boundary, and optional labels.}

\item{triangleWidth}{Fixed triangle width in millimetres. Very short ranges
cap the triangle at 45 percent of their displayed width.}

\item{rangeHeight}{Range height relative to one overlap lane.}

\item{geometry}{Range geometry. \code{"pointed"} retains the fixed-tip shape;
\code{"rectangle"} draws a simple rectangular range.}

\item{directionIndicator}{Use \code{"arrow"} to add a subtle \code{>} or \code{<} strand
glyph inside each sufficiently wide range, or use \code{"none"}.}

\item{directionColor, directionFontsize}{Appearance of the strand glyph.}

\item{stacking}{Either \code{"squish"} for deterministic non-overlapping lanes or
\code{"dense"} for one lane.}

\item{showLabels}{Draw range labels.}

\item{labelPosition}{Draw labels \code{"inside"} or immediately \code{"above"} ranges.}

\item{labelFontsize}{Label size in points.}

\item{alpha}{Range alpha.}

\item{borderWidth}{Outline width.}

\item{showBoundary}{Draw a vertical boundary at the left edge of the panel.}

\item{boundaryWidth, boundaryInset}{Width and fractional end inset of the
left boundary.}

\item{...}{Additional Gviz display parameters.}
}
\value{
A \code{Gviz::CustomTrack}.
}
\description{
Draw stranded genomic ranges as compact rectangles with a shallow triangular
point. The point has a fixed physical width and the same height as the body,
avoiding the oversized, span-dependent arrowheads used by standard Gviz
annotation arrows. Overlapping ranges are assigned deterministic lanes.
}
\examples{
hits <- GenomicRanges::GRanges(
    "chr1", IRanges::IRanges(c(10, 35), width = c(20, 15)),
    strand = c("+", "-"), feature = c("enhancer", "promoter")
)
DirectionalRangeTrack(
    hits,
    fill = c(enhancer = "#0072B2", promoter = "#D55E00")
)
}
